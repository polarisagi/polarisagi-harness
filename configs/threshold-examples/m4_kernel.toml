# Example threshold overrides for m4_kernel.toml
# To use, copy this file to ~/.polarisagi/harness/config/ or point POLARIS_THRESHOLDS_DIR to this directory.

max_replan_attempts = 3
default_budget = 50000
max_steps = 10
tier0_max_concurrent = 4
suspend_idle_threshold_minutes = 5
'plan_dag.max_nodes' = 50
'plan_dag.max_depth' = 10
'l3_watchdog.max_per_hour' = 10
'world_model.skip_threshold' = 0.8
'snapshot.interval_steps' = 1000
'snapshot.retention_count' = 5
