{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "markdown_render",
  "description": "Render Markdown text to HTML output.",
  "type": "object",
  "properties": {
    "markdown":   { "type": "string", "description": "The Markdown source text to render." },
    "extensions": { "type": "array",  "items": { "type": "string" }, "description": "List of Markdown extensions to enable (e.g. 'tables', 'strikethrough', 'task-list')." }
  },
  "required": ["markdown"]
}
