{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "json_parse",
  "description": "Parse a JSON string into structured data and optionally validate it against a JSON Schema.",
  "type": "object",
  "properties": {
    "data":   { "type": "string", "description": "The raw JSON string to parse." },
    "schema": { "type": "object", "description": "Optional JSON Schema (Draft 2020-12) to validate the parsed data against." }
  },
  "required": ["data"]
}
