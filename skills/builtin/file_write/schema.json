{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "file_write",
  "description": "Write or overwrite a file at a given path within allowed directories.",
  "type": "object",
  "properties": {
    "path":      { "type": "string",  "description": "Absolute path of the file to write. Must be within allowed directories." },
    "content":   { "type": "string",  "description": "Content to write into the file." },
    "overwrite": { "type": "boolean", "default": false, "description": "If false (default), the operation fails when the file already exists." }
  },
  "required": ["path", "content"]
}
