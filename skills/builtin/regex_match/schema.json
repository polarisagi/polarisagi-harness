{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "regex_match",
  "description": "Match text against a regular expression pattern and return all matches with capture groups.",
  "type": "object",
  "properties": {
    "pattern": { "type": "string", "description": "Regular expression pattern (RE2 syntax)." },
    "text":    { "type": "string", "description": "Input text to search for matches." },
    "flags":   { "type": "array",  "items": { "type": "string" }, "description": "Regex flags to apply (e.g. ['i'] for case-insensitive, ['m'] for multiline)." }
  },
  "required": ["pattern", "text"]
}
