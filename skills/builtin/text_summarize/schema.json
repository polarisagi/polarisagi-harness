{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "text_summarize",
  "description": "Generate a concise summary of input text using the configured language model.",
  "type": "object",
  "properties": {
    "text":       { "type": "string",                                              "description": "The input text to summarize." },
    "max_length": { "type": "integer", "default": 200,                             "description": "Maximum length of the summary in words." },
    "style":      { "type": "string",  "enum": ["concise", "detailed", "bullets"], "description": "Summary style: concise (one sentence), detailed (paragraph), or bullets (bullet list)." }
  },
  "required": ["text"]
}
